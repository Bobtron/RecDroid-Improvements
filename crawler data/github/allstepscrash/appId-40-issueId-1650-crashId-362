<td class="d-block comment-body markdown-body  js-comment-body">

          <p>In <code>setup_class</code> and <code>setup_generated_tests</code>, calling <code>expects</code> APIs would cause crashes.<br>
Also in <code>teardown_class</code>, <code>expects</code> APIs would probably attach errors to the wrong record.</p>
      </td>