<td class="d-block comment-body markdown-body  js-comment-body">

          <p>Calling first removeEvents function and after to refreshView, raise error "Cannot read property 'getDate' of undefined". I think this happen becouse events array is changed by only id's events.</p>
<p>_removeEventsWithIds: function(sArrRemoveIds):<br>
<code>oArrEventsTemp.push(oTempEvent.id);</code></p>
<p>Changing by:<br>
<code>oArrEventsTemp.push(oTempEvent);</code></p>
<p>It does not happen</p>
<p>This is a bug or I don't understanding correctly ?</p>
      </td>